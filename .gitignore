__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# Machine-local agent wiring (command symlinks, settings.local.json) — never committed.
.claude/

# codegraph index — machine-local, self-provisioned by `devloop pack` (funloops#28).
.codegraph/
